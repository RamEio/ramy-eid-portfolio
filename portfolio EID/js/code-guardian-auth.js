class CodeGuardianAuth{constructor(){this.isAuthenticated=false;this.maxAttempts=3;this.attempts=0;this.lockoutTime=5*60*1000;this.lockoutUntil=0;this.passwordHash='sha256_hash_placeholder';this.init()}init(){console.log('🔐 Code Guardian Auth:Initializing authentication system...');this.checkExistingAuth();this.setupAuthUI();console.log('✅ Code Guardian Auth:Authentication system initialized')}checkExistingAuth(){const authToken=localStorage.getItem('codeGuardianAuth');const authTime=localStorage.getItem('codeGuardianAuthTime');if(authToken&&authTime){const authAge=Date.now()-parseInt(authTime);const sessionTimeout=30*60*1000;if(authAge<sessionTimeout){this.isAuthenticated=true;console.log('🔐 Code Guardian Auth:Existing session found')}else{localStorage.removeItem('codeGuardianAuth');localStorage.removeItem('codeGuardianAuthTime');console.log('🔐 Code Guardian Auth:Session expired')}}}setupAuthUI(){const dashboard=document.getElementById('dashboard-content');const authContainer=document.getElementById('auth-container');if(this.isAuthenticated){if(dashboard)dashboard.style.display='block';if(authContainer)authContainer.style.display='none'}else{if(dashboard)dashboard.style.display='none';if(authContainer)authContainer.style.display='block';this.setupAuthForm()}}setupAuthForm(){const authForm=document.getElementById('auth-form');if(authForm){authForm.addEventListener('submit',(e)=>{e.preventDefault();this.handleLogin()})}}handleLogin(){const passwordInput=document.getElementById('auth-password');const password=passwordInput.value;if(this.isLockedOut()){this.showError('Account temporarily locked. Please try again later.');return}if(this.validatePassword(password)){this.authenticate()}else{this.handleFailedAttempt()}passwordInput.value=''}isLockedOut(){return Date.now()<this.lockoutUntil}validatePassword(password){const hashedPassword=this.simpleHash(password);return hashedPassword===this.passwordHash}simpleHash(str){let hash=0;for(let i=0;i<str.length;i++){const char=str.charCodeAt(i);hash=((hash<<5)-hash)+char;hash=hash&hash}return hash.toString()}authenticate(){this.isAuthenticated=true;this.attempts=0;const authToken=this.generateAuthToken();localStorage.setItem('codeGuardianAuth',authToken);localStorage.setItem('codeGuardianAuthTime',Date.now().toString());this.setupAuthUI();console.log('🔐 Code Guardian Auth:Authentication successful');this.showSuccess('Authentication successful. Welcome to Code Guardian Dashboard.');this.initializeDashboard()}handleFailedAttempt(){this.attempts++;if(this.attempts>=this.maxAttempts){this.lockoutUntil=Date.now()+this.lockoutTime;this.showError(`Too many failed attempts. Account locked for ${this.lockoutTime/60000}minutes.`);console.warn('🔐 Code Guardian Auth:Account locked due to multiple failed attempts')}else{const remainingAttempts=this.maxAttempts-this.attempts;this.showError(`Invalid password. ${remainingAttempts}attempts remaining.`)}}generateAuthToken(){return 'cg_'+Date.now()+'_'+Math.random().toString(36).substr(2,9)}showError(message){const errorDiv=document.getElementById('auth-error');if(errorDiv){errorDiv.textContent=message;errorDiv.style.display='block';setTimeout(()=>{errorDiv.style.display='none'},5000)}}showSuccess(message){const successDiv=document.getElementById('auth-success');if(successDiv){successDiv.textContent=message;successDiv.style.display='block';setTimeout(()=>{successDiv.style.display='none'},3000)}}initializeDashboard(){if(window.CodeGuardianMonitor){window.codeGuardianMonitor=new window.CodeGuardianMonitor()}if(window.CodeGuardianFSIntegration){window.codeGuardianFSIntegration=new window.CodeGuardianFSIntegration()}}logout(){this.isAuthenticated=false;localStorage.removeItem('codeGuardianAuth');localStorage.removeItem('codeGuardianAuthTime');this.attempts=0;this.lockoutUntil=0;this.setupAuthUI();console.log('🔐 Code Guardian Auth:Logout successful')}getAuthStatus(){return{isAuthenticated:this.isAuthenticated,attempts:this.attempts,maxAttempts:this.maxAttempts,isLockedOut:this.isLockedOut(),lockoutRemaining:Math.max(0,this.lockoutUntil-Date.now())}}}document.addEventListener('DOMContentLoaded',()=>{window.codeGuardianAuth=new CodeGuardianAuth()});if(typeof module!=='undefined'&&module.exports){module.exports=CodeGuardianAuth}